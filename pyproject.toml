[project]
name = "jellyvision-programming-exercise"
version = "1.0.0"
description = "Jellyvision programming exercise as part of the interview process"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.2",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.14.14",
]
